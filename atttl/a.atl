module testModels;
create OUT: reo from IN: reo;

rule def2mod {
	from
		b: reo!Module
	to
		m: reo!Module-- (
		--	connectors <- b.connectors
		--)
	do {
		m.debug('def to mod');
	--	m.debug('def to mod ' + b.connectors); 
	}
}
